#include <cstdlib>
#include <iostream>
#include <ctime>

using namespace std;

int main(int argc, char** argv) {
    srand(time(NULL));
    //long epochtime = 0;
    //epochtime = time(NULL);
    //printf("%d\n", epochtime);
    if (rand()==728666055) // sometime before the homework deadline
        cout << "ec521" << endl;
}
